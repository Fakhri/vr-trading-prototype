# FRONTEND

This is the "thing" that should be rendered inside the VR app that is web-based.
Fetch data directly from the sponsors API then display it to the clients.
Or, use the data from the backend if there is any pre-processing needed
